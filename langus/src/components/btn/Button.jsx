import style from './btn/button.module.scss';

export default function ButtonS() {
  return (
    <>{
      <div className={`${style.buttonS}`}><span className={`${style.glowingtxt}`}>C<span className={`${style.faultyletter}`}>L</span>ICK</span></div>

    }</>
  )
}